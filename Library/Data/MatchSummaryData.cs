

namespace VedAstro.Library
{
    /// <summary>
    /// Simple data structure to hold summarized data for match compatibility report
    /// </summary>
    public readonly record struct MatchSummaryData(string heartIcon, string scoreColor, string scoreSummary);
}
